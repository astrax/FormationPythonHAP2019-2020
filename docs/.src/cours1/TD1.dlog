translating doconce text in TD1.do.txt to pdflatex
open file with encoding utf-8

*** Exercise: Les variables
    label{None}
    could be Problem (no refs beyond the exercise itself)

*** Exercise: Fonction `input()`
    label{None}
    could be Problem (no refs beyond the exercise itself)

*** Exercise: Corriger l'erreur dans le code
    label{None}
    could be Problem (no refs beyond the exercise itself)
found info about 3 exercises
*** warning: !bc ipy used for IPython sessions, but
    ipython is not supported for syntax highlighting!
    install:
    git clone https://hplbit@bitbucket.org/hplbit/pygments-ipython-console.git; cd pygments-ipython-console; sudo python setup.py install
no lexer for alias 'ipy' found
output in TD1.tex
translating doconce text in TD1.do.txt to pdflatex
open file with encoding utf-8

*** Exercise: Les variables
    label{None}
    could be Problem (no refs beyond the exercise itself)

*** Exercise: Fonction `input()`
    label{None}
    could be Problem (no refs beyond the exercise itself)

*** Exercise: Corriger l'erreur dans le code
    label{None}
    could be Problem (no refs beyond the exercise itself)
found info about 3 exercises
*** warning: !bc ipy used for IPython sessions, but
    ipython is not supported for syntax highlighting!
    install:
    git clone https://hplbit@bitbucket.org/hplbit/pygments-ipython-console.git; cd pygments-ipython-console; sudo python setup.py install
no lexer for alias 'ipy' found
output in TD1.tex
translating doconce text in TD1.do.txt to html
open file with encoding utf-8

*** Exercise: Les variables
    label{None}
    could be Problem (no refs beyond the exercise itself)

*** Exercise: Fonction `input()`
    label{None}
    could be Problem (no refs beyond the exercise itself)

*** Exercise: Corriger l'erreur dans le code
    label{None}
    could be Problem (no refs beyond the exercise itself)
found info about 3 exercises
*** warning: !bc ipy used for IPython sessions, but
    ipython is not supported for syntax highlighting!
    install:
    git clone https://hplbit@bitbucket.org/hplbit/pygments-ipython-console.git; cd pygments-ipython-console; sudo python setup.py install
no lexer for alias 'ipy' found
output in TD1.html
translating doconce text in TD1.do.txt to pdflatex
open file with encoding utf-8

*** Exercise: Les variables
    label{None}
    could be Problem (no refs beyond the exercise itself)

*** Exercise: Fonction `input()`
    label{None}
    could be Problem (no refs beyond the exercise itself)

*** Exercise: Corriger l'erreur dans le code
    label{None}
    could be Problem (no refs beyond the exercise itself)
found info about 3 exercises
*** warning: !bc ipy used for IPython sessions, but
    ipython is not supported for syntax highlighting!
    install:
    git clone https://hplbit@bitbucket.org/hplbit/pygments-ipython-console.git; cd pygments-ipython-console; sudo python setup.py install
no lexer for alias 'ipy' found
output in TD1.tex
translating doconce text in TD1.do.txt to pdflatex
open file with encoding utf-8

*** Exercise: Les variables
    label{None}
    could be Problem (no refs beyond the exercise itself)

*** Exercise: Fonction `input()`
    label{None}
    could be Problem (no refs beyond the exercise itself)

*** Exercise: Corriger l'erreur dans le code
    label{None}
    could be Problem (no refs beyond the exercise itself)
found info about 3 exercises
*** warning: !bc ipy used for IPython sessions, but
    ipython is not supported for syntax highlighting!
    install:
    git clone https://hplbit@bitbucket.org/hplbit/pygments-ipython-console.git; cd pygments-ipython-console; sudo python setup.py install
no lexer for alias 'ipy' found
output in TD1.tex
translating doconce text in TD1.do.txt to html
open file with encoding utf-8

*** Exercise: Les variables
    label{None}
    could be Problem (no refs beyond the exercise itself)

*** Exercise: Fonction `input()`
    label{None}
    could be Problem (no refs beyond the exercise itself)

*** Exercise: Corriger l'erreur dans le code
    label{None}
    could be Problem (no refs beyond the exercise itself)
found info about 3 exercises
*** warning: !bc ipy used for IPython sessions, but
    ipython is not supported for syntax highlighting!
    install:
    git clone https://hplbit@bitbucket.org/hplbit/pygments-ipython-console.git; cd pygments-ipython-console; sudo python setup.py install
no lexer for alias 'ipy' found
output in TD1.html
translating doconce text in TD1.do.txt to pdflatex
open file with encoding utf-8

*** Exercise: Les variables
    label{None}
    could be Problem (no refs beyond the exercise itself)

*** Exercise: Fonction `input()`
    label{None}
    could be Problem (no refs beyond the exercise itself)

*** Exercise: Corriger l'erreur dans le code
    label{None}
    could be Problem (no refs beyond the exercise itself)
found info about 3 exercises
*** warning: !bc ipy used for IPython sessions, but
    ipython is not supported for syntax highlighting!
    install:
    git clone https://hplbit@bitbucket.org/hplbit/pygments-ipython-console.git; cd pygments-ipython-console; sudo python setup.py install
no lexer for alias 'ipy' found
output in TD1.tex
translating doconce text in TD1.do.txt to pdflatex
open file with encoding utf-8

*** Exercise: Les variables
    label{None}
    could be Problem (no refs beyond the exercise itself)

*** Exercise: Fonction `input()`
    label{None}
    could be Problem (no refs beyond the exercise itself)

*** Exercise: Corriger l'erreur dans le code
    label{None}
    could be Problem (no refs beyond the exercise itself)
found info about 3 exercises
*** warning: !bc ipy used for IPython sessions, but
    ipython is not supported for syntax highlighting!
    install:
    git clone https://hplbit@bitbucket.org/hplbit/pygments-ipython-console.git; cd pygments-ipython-console; sudo python setup.py install
no lexer for alias 'ipy' found
output in TD1.tex
translating doconce text in TD1.do.txt to html
open file with encoding utf-8

*** Exercise: Les variables
    label{None}
    could be Problem (no refs beyond the exercise itself)

*** Exercise: Fonction `input()`
    label{None}
    could be Problem (no refs beyond the exercise itself)

*** Exercise: Corriger l'erreur dans le code
    label{None}
    could be Problem (no refs beyond the exercise itself)
found info about 3 exercises
*** warning: !bc ipy used for IPython sessions, but
    ipython is not supported for syntax highlighting!
    install:
    git clone https://hplbit@bitbucket.org/hplbit/pygments-ipython-console.git; cd pygments-ipython-console; sudo python setup.py install
no lexer for alias 'ipy' found
output in TD1.html
